# TEMPLATE — Ne jamais committer de vraies valeurs.
# Créer les secrets hors-git : `kubectl create secret generic pcb3-secret ...`
# ou via sealed-secrets / external-secrets / SOPS.
apiVersion: v1
kind: Secret
metadata:
  name: pcb3-secret
  namespace: pcb-ai-v3
  labels:
    app.kubernetes.io/name: pcb-ai-designer
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  JWT_SECRET_KEY: ""
  OPENAI_API_KEY: ""
  ZAI_API_KEY: ""
  DATABASE_PASSWORD: ""
  S3_ACCESS_KEY: ""
  S3_SECRET_KEY: ""
  JLCPCB_API_KEY: ""
  PCBWAY_API_KEY: ""
