# API FastAPI (api_gateway) — défaut http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
# WebSocket live (events agents) — défaut ws://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
# Tenant par défaut envoyé dans les headers X-Tenant-Id
NEXT_PUBLIC_TENANT_ID=default
